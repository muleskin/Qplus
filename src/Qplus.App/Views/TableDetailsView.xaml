<UserControl x:Class="Qplus.App.Views.TableDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Bg}">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{DynamicResource Brush.Panel}" Padding="8,5"
                BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="TitleText" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock x:Name="SubtitleText" VerticalAlignment="Center" Margin="10,0,0,0" Opacity="0.7"/>
                <Button Content="⟳ Refresh all" Click="RefreshAll_Click" Margin="16,0,0,0" Padding="8,2"/>
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="16,0,0,0" Opacity="0.85"/>
            </StackPanel>
        </Border>

        <TabControl x:Name="DetailTabs" Background="{DynamicResource Brush.Bg}"
                    SelectionChanged="DetailTabs_SelectionChanged"/>
    </DockPanel>
</UserControl>
